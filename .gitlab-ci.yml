# Import tasks
include:
  - project: 'devandgo/gitlab-templates'
    file: '/ci/tag-semantic-release.yml'

# Define stages
stages:
  - tag

# Overrides
tag:
  environment:
    url: https://npm.devandgo.com/-/web/detail/@devandgo/headless-browser
